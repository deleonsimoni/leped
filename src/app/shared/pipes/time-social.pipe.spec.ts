import { TimeSocialPipe } from './time-social.pipe';

describe('TimeSocialPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeSocialPipe();
    expect(pipe).toBeTruthy();
  });
});
